## Phase 1 Foundation — Backend Complete (2026-02-21)

### What was built
- Phoenix 1.8.1 project skeleton with Elixir 1.19.2 / OTP 28
- 8 database migrations (citext, users, user_tokens, channels, subscriptions, messages, dm_conversations, read_cursors)
- 7 Ecto schemas with full changesets, validations, and associations
- Accounts context: registration, password auth, session tokens
- Auth module: JWT access/refresh tokens via Guardian, token rotation with 10s grace window, family invalidation on replay
- Chat context: channels (create/join/leave), messages (permission-checked, Snowflake ID, PubSub broadcast), DMs (user ordering invariant), read cursors (upsert-based unread tracking)
- Snowflake ID generator GenServer: 64-bit IDs with advisory lock, clock drift handling, sequence overflow
- Rate limiter: pure functional token bucket (no GenServer)
- Permissions module: role hierarchy (owner > admin > member > viewer > nil)
- ExMachina test factory with builders for all schemas
- docker-compose.yml with pgvector Postgres (port 5433) and Redis

### Test coverage
- 66 tests (0 failures), mix credo --strict (0 issues), mix compile --warnings-as-errors clean

### Lessons learned

1. **Elixir Bitwise operators require explicit import**: `<<<`, `>>>`, `|||`, `&&&` are not available by default. Must `import Bitwise` in any module using them, including test files.

2. **Ecto schema type `:text` does not exist**: In migrations you use `:text` for the DB column type, but in Ecto schemas you must use `:string` for both varchar and text columns. `:text` is only valid in migrations.

3. **`:utc_datetime_usec` requires microsecond precision (6 decimal places)**: `DateTime.from_unix!(ms, :millisecond)` creates a DateTime with precision=3 (milliseconds), e.g. `~U[2026-02-21 23:23:22.291Z]`. Ecto's `:utc_datetime_usec` rejects this. Fix: use `DateTime.from_unix!(ms * 1000, :microsecond)` to get precision=6. Note: `DateTime.truncate(:microsecond)` does NOT upgrade precision — it only truncates downward and preserves the original precision marker.

4. **`replace_all` in edits catches alias lines too**: When doing a global replace of `Slackex.Repo` -> `Repo`, the `alias Slackex.Repo` line also gets replaced to `alias Repo` which is invalid. Always be careful with global replacements near alias declarations.

5. **Phoenix 1.8 no longer includes Swoosh by default but still generates mailer code**: After `mix phx.new`, manually remove `lib/slackex/mailer.ex` and all Swoosh config if email is not needed.

6. **Credo alias ordering**: Credo --strict checks that aliases within a group are alphabetically ordered. Multi-alias `{A, B, C}` must also be sorted. `Slackex.Accounts` should come before `Slackex.Accounts.{Auth, Guardian}` which comes before `Slackex.Repo`.

7. **Credo nested module aliases**: When a module is used more than once, Credo prefers a top-level `alias` rather than inline fully-qualified names. Use `# credo:disable-for-this-file Credo.Check.Design.AliasUsage` for Phoenix-generated files.

8. **Credo function nesting depth**: `with` clauses containing nested `case`/`if` easily exceed max depth of 2. Extract the inner logic into private functions to flatten the nesting.

### What's next
- LiveView chat interface (Step 7): registration/login forms, real-time message list, channel sidebar
- Boundary compile-time checks
- Git hooks and bin/setup script

---

## Phase 1 Foundation — API & WebSocket Layer (2026-02-21)

### What was built
- Shared JSON serializers: UserJSON, ChannelJSON, MessageJSON (Snowflake IDs as strings for JS safety)
- API auth pipeline: Guardian plug pipeline (VerifyHeader, EnsureAuthenticated, LoadResource) with JSON 401 error handler
- API auth controller: POST /api/auth/login (email+password -> tokens+user), POST /api/auth/refresh (token rotation)
- API bootstrap controller: GET /api/bootstrap (JWT-protected, returns user+channels+DMs+unread_counts)
- UserSocket: JWT-authenticated WebSocket with chat:* and dm:* channel routes
- ChatChannel: join (role check, load messages, mark as read), send message (broadcast via Phoenix Channel)
- DMChannel: join (participant check, load messages), send message (broadcast)
- ChannelCase test support module for Phoenix Channel tests
- Chat context additions: get_dm/1, list_dm_messages/2

### Test coverage
- 105 tests (0 failures), mix credo --strict (0 issues), mix compile --warnings-as-errors clean
- 39 new behavioral tests covering: API login, refresh, bootstrap, WebSocket connect/join/send, serializer correctness

### Bugs found and fixed during testing

1. **Guardian.Plug.Pipeline requires `otp_app`**: `use Guardian.Plug.Pipeline` without `otp_app: :slackex` causes a RuntimeError at runtime. Not caught by compiler — only surfaced when tests actually exercised the pipeline.

2. **Phoenix JSON view namespace must match controller namespace**: Controllers in `SlackexWeb.API.*` expect JSON views in the same namespace. Defining serializers as `SlackexWeb.MessageJSON` instead of `SlackexWeb.API.MessageJSON` causes Phoenix to fail to find the view module. The convention is: controller `SlackexWeb.API.AuthController` renders via `SlackexWeb.API.AuthJSON`.

3. **`to_string/1` on non-atom Guardian error tuples**: `Auth.refresh_api_token/1` can return `{:error, %ErlangError{}}` for malformed tokens. Calling `to_string(reason)` on a struct raises `Protocol.UndefinedError`. Guard clause needed to handle non-atom/binary error reasons.

4. **Nil sender crashes serializer**: `MessageJSON.data/1` called `UserJSON.data(sender)` without nil guard. When `sender` is nil (deleted user), `UserJSON.data/1` pattern match fails with `FunctionClauseError`. Fixed with a `serialize_sender/1` private function that returns nil for nil input.

### Lessons learned

9. **Phoenix JSON view naming convention**: For controllers namespaced under `SlackexWeb.API.*`, the JSON view must also be under `SlackexWeb.API.*`. Phoenix resolves view modules by replacing `Controller` with `JSON` in the controller module name.

10. **Guardian.Plug.Pipeline needs `otp_app`**: Always pass `otp_app: :your_app` to `use Guardian.Plug.Pipeline`. Without it, Guardian cannot find the configuration and raises at runtime, not compile time.

11. **Serializers need nil guards for optional associations**: Any serializer field that calls another serializer must guard against nil. Use a private helper: `defp serialize_foo(nil), do: nil; defp serialize_foo(foo), do: FooJSON.data(foo)`.

12. **Snowflake GenServer is auto-started in test**: `Slackex.Infrastructure.Snowflake` is in the application supervision tree. Do NOT call `start_supervised!` in test setup — it will raise `{:already_started, pid}`. Only start it manually if the application supervisor is not running.

### What's next
- LiveView chat interface (Step 7): registration/login forms, real-time message list, channel sidebar
- Boundary compile-time checks
- Git hooks and bin/setup script

---

## Phase 2 CQRS Core — Steps 1–3 + Supervisor (2026-02-22)

### What was built
- **`Slackex.Cache.Local`** (`lib/slackex/cache/local.ex`): ETS-backed message cache GenServer. Public table (`:set`, `:public`, `:named_table`, `read_concurrency: true`, `write_concurrency: true`). LRU eviction via monotonic timestamps when channel count exceeds 1,000. Messages stored newest-first, returned chronologically. Direct ETS reads; GenServer mediates writes for atomicity.
- **`Slackex.Pipeline.BatchWriter`** (`lib/slackex/pipeline/batch_writer.ex`): Batch DB insert module. `insert_batch/1` maps message maps to rows (deriving `inserted_at` from Snowflake ID via `DateTime.from_unix!(ms * 1000, :microsecond)`), runs `Repo.insert_all("messages", entries, on_conflict: :nothing)`. `async_insert_batch/2` dispatches via `Task.Supervisor.start_child(Slackex.WriteSupervisor, ...)` and sends `{:batch_result, ref, :ok | {:error, reason}}` to caller.
- **`Slackex.Messaging.ChannelServer`** (`lib/slackex/messaging/channel_server.ex`): GenServer per active channel/DM. Bounded 200-message in-memory queue, 2-second async batch flush via BatchWriter, per-user per-channel rate limiting (10 msg/s token bucket), PubSub broadcast on send, 30-minute idle hibernation, batch retry with telemetry on max-retries-exceeded.
- **`Slackex.Messaging.ChannelSupervisor`** (`lib/slackex/messaging/channel_supervisor.ex`): DynamicSupervisor for ChannelServer processes. `ensure_started/2` looks up via Registry, starts child if not found, handles `{:error, {:already_started, pid}}` race.
- **`Slackex.Messaging`** (`lib/slackex/messaging/messaging.ex`): Context facade. `send_message/4`, `send_dm/4` (with participant validation), `get_recent_messages/2` (ChannelServer → DB fallback), PubSub subscribe/unsubscribe helpers, `broadcast_typing/2`, `channel_count/0`.
- **Application supervisor updated** (`lib/slackex/application.ex`): Added `Slackex.Cache.Local`, `{Registry, keys: :unique, name: Slackex.Messaging.ChannelRegistry}`, `Slackex.Messaging.ChannelSupervisor`, `{Task.Supervisor, name: Slackex.WriteSupervisor}`.

### Test coverage
- 157 tests (0 failures), mix credo --strict (0 issues), mix compile --warnings-as-errors clean
- 52 new behavioral tests covering: ETS cache put/get/invalidate/stats/LRU, batch insert/async, ChannelServer message flow/rate limiting/PubSub, Messaging context send/DM/subscribe/typing/channel_count/ensure_started

### Lessons learned

13. **ETS named tables are owned by the creating process**: When `Cache.Local` is added to the application supervisor, `start_supervised!(Local)` in tests raises `{:already_started, pid}`. Fix: use `:ets.delete_all_objects(:slackex_message_cache)` in test `setup` to clear state between tests rather than restarting the GenServer.

14. **Task.Supervisor start_supervised! conflict**: Once `{Task.Supervisor, name: Slackex.WriteSupervisor}` is in the app supervisor, test setups that call `start_supervised!({Task.Supervisor, name: Slackex.WriteSupervisor})` fail with `:already_started`. Remove the per-test `start_supervised!` and rely on the running supervisor.

15. **DynamicSupervisor child cleanup in tests**: Calling `GenServer.stop(pid, :normal)` on a `:permanent` DynamicSupervisor child causes a restart. Use `DynamicSupervisor.terminate_child(ChannelSupervisor, pid)` instead — this removes the child from the supervisor without triggering a restart.

16. **Rate limiter affects large-batch tests**: ChannelServer rate-limits at 10 msg/s per user. Tests that try to send 200+ messages from a single user will hit `:error, :rate_limited` after 10. Bounded-queue tests should either use many distinct users or be removed in favour of testing the limit via direct ETS/queue inspection.

### Phase 2 steps remaining
- Steps 8-9: Versioned envelope contract, write rejection normalization
- LiveView chat UI (from Phase 1 remaining work)

---

## Phase 2 CQRS — Steps 4–7: Presence, History, Oban, Write Path Migration (2026-02-22)

### What was built
- **`SlackexWeb.Presence`** (`lib/slackex_web/presence.ex`): Phoenix Presence module for tracking online users per channel. Uses `Phoenix.Presence` with `pubsub_server: Slackex.PubSub`. Added to application supervisor after PubSub.
- **`Slackex.Search.HistoryLoader`** (`lib/slackex/search/history_loader.ex`): CQRS read side with cache cascade. `recent/2` checks ETS cache first, falls back to DB with backfill. `before/3` always reads from DB for older messages. Returns `{:ok, messages}` in chronological order. Target-aware tuples `{:channel, id}` and `{:dm, id}`.
- **`Slackex.Workers.CacheWarmer`** (`lib/slackex/workers/cache_warmer.ex`): Oban worker (queue: default, max_attempts: 1). Runs hourly via Oban.Plugins.Cron. Finds channels with messages in the last hour via `Chat.list_active_channels/1`, ensures their ChannelServer is started (which rehydrates cache on init).
- **`Chat.list_active_channels/1`** added to `lib/slackex/chat/chat.ex`: Queries channels with recent message activity using a subquery on the messages table.
- **Oban integration**: Added `{:oban, "~> 2.18"}` dependency, migration (version 12), config with queues (default: 10, notifications: 20, embeddings: 5), Pruner plugin, and Cron plugin with hourly CacheWarmer.
- **Write path migration** (Step 6): ChatChannel and DMChannel now route through `Messaging.send_message/3` and `Messaging.send_dm/3` instead of direct `Chat` context calls. Removed `broadcast!/3` from both channels — ChannelServer already broadcasts via PubSub, channels subscribe and push to client via `handle_info({:new_message, msg})`.

### Test coverage
- 183 tests (0 failures), mix credo --strict (0 issues), mix compile --warnings-as-errors clean
- 26 new behavioral tests covering: Presence tracking, typing broadcast, HistoryLoader cache hit/miss/before pagination, CacheWarmer perform/active channels query, ChatChannel/DMChannel write path migration

### Lessons learned

17. **Duplicate broadcast trap when migrating write paths**: When migrating from `Chat.send_message` (direct DB write) to `Messaging.send_message` (ChannelServer), you must remove the channel's `broadcast!/3` call. The ChannelServer already broadcasts via PubSub — keeping both causes duplicate messages. Instead, subscribe the channel process to the PubSub topic on join and handle `{:new_message, msg}` to push to the client.

18. **HistoryLoader returns different shapes than Chat context**: `Chat.list_messages/2` returns Ecto structs with preloaded `:sender`. `HistoryLoader.recent/2` returns cached maps (no `:sender`). For the initial channel join payload (which needs full serialization via MessageJSON.data/1), keep using `Chat.list_messages` directly. Use HistoryLoader for the CQRS read path where map shapes suffice.

19. **Oban test config `testing: :inline`**: This makes all Oban jobs execute synchronously in tests. Without it, jobs are enqueued but never run, causing test assertions on side effects to fail.

20. **struct_to_map for cache backfill**: When backfilling ETS cache from DB results, Ecto structs must be converted to maps via `Map.from_struct/1`. Direct ETS storage of structs works but is inconsistent with the map-based cache contract used by ChannelServer.

### Phase 2 steps remaining
- LiveView chat UI (from Phase 1 remaining work)
- Auto-scroll JS hook (deferred to Phase 1 Step 7 — requires LiveView)

---

## Phase 2 CQRS — Steps 8–9: Envelope Contract & Write Rejection Normalization (2026-02-22)

### What was built
- **`Slackex.Messaging.Envelope`** (`lib/slackex/messaging/envelope.ex`): Versioned envelope module wrapping all realtime payloads. `wrap/4` converts `{:channel, id}` or `{:dm, id}` target tuples into `%{v: 1, event, target: %{type, id}, payload, meta: %{sent_at, correlation_id}}`. `unwrap/1` extracts event, target, payload for pattern matching.
- **ChannelServer updates** (`lib/slackex/messaging/channel_server.ex`): Broadcasts now use `{:envelope, envelope}` instead of raw `{:new_message, message}`. Added `check_backpressure/1` — returns `{:error, :backpressure}` when `pending_writes >= @max_pending_writes` (was silently dropping). Added `validate_content/1` — returns `{:error, :invalid_content}` for empty or >4000 char content. Both checks run before Snowflake ID generation in the `with` chain.
- **Messaging context** (`lib/slackex/messaging/messaging.ex`): `broadcast_typing/2` now wraps typing events in envelope.
- **ChatChannel + DMChannel** (`lib/slackex_web/channels/`): Handle `{:envelope, %{event: "message.new"}}` and `{:envelope, %{event: "typing"}}` patterns. Push events as `"message.new"` and `"typing"` (not `"new_message"`). All write rejection errors normalized to `%{reason: string, message: string}` shape with specific clauses for `:rate_limited`, `:backpressure`, `:unauthorized`, `:invalid_content`.
- **DMChannel PubSub bug fix**: Added missing `Phoenix.PubSub.subscribe(Slackex.PubSub, "dm:#{dm_id}")` in `join/3` — DM real-time message delivery from ChannelServer was broken.
- **Contract tests** (`test/slackex_web/channels/envelope_contract_test.exs`): 12 contract tests tagged `@tag :contract` covering write rejection shapes and envelope delivery for both ChatChannel and DMChannel.

### Test coverage
- 206 tests (0 failures), mix credo --strict (0 issues), mix compile --warnings-as-errors clean
- 23 new tests: 6 envelope unit, 5 ChannelServer validation, 12 contract tests
- Contract tests excluded from default run; `mix test --include contract` runs all 206

### Lessons learned

21. **Silent drops become invisible bugs**: ChannelServer was silently dropping messages when `pending_writes` was full — the caller got `{:ok, message}` even though the message would never be persisted. The spec correctly required `{:error, :backpressure}` so the client knows the message was rejected. Always fail visibly at system boundaries.

22. **DMChannel PubSub subscription was missing**: DMChannel.join did not call `Phoenix.PubSub.subscribe/2`, so `handle_info({:envelope, _})` would never fire for DM messages delivered by ChannelServer. ChatChannel had the subscription but DMChannel didn't — copy-paste divergence. The bug was masked in tests because Phoenix channel test helpers may auto-subscribe. Always verify PubSub wiring with integration-level assertions.

23. **Contract tests as executable protocol spec**: Tagging tests with `@tag :contract` and excluding from default run (`ExUnit.configure(exclude: [:contract])`) creates a clear separation between implementation tests and protocol-level contracts. `mix test` runs fast behavioral tests; `mix test --include contract` adds protocol verification. This pattern from the spec (Section 6.4) avoids coupling fast CI feedback with slower contract checks.

24. **Content validation belongs in the GenServer, not the schema**: ChannelServer bypasses Ecto changesets (builds maps directly for performance). This means Message schema's 1-4000 char validation never runs. Content validation must be duplicated in the GenServer's write path. The check must run before Snowflake ID generation to avoid wasting IDs on invalid messages.

### What's next
- Phase 3: Scalability (time-partitioned messages, Redis caching, connection pooling)
- Phase 4: Search (full-text search, pgvector embeddings)
- Boundary compile-time checks, Tidewave setup, git hooks

---

## Phase 1 Step 7 — LiveView Chat UI (2026-02-22)

### What was built
- **`SlackexWeb.UserAuth`** (`lib/slackex_web/user_auth.ex`): Full auth plug module following `phx.gen.auth` patterns. `fetch_current_user/2` plug loads user from session token. Three `on_mount` callbacks: `:mount_current_user` (assigns current_user), `:ensure_authenticated` (redirects to login if not authenticated), `:redirect_if_authenticated` (redirects to /chat if already logged in). Cookie-based remember-me support. `log_in_user/3` and `log_out_user/1` for session lifecycle.
- **`SlackexWeb.UserSessionController`** (`lib/slackex_web/controllers/user_session_controller.ex`): POST /users/log-in (email+password auth, delegates to UserAuth.log_in_user), DELETE /users/log-out.
- **`SlackexWeb.AuthLive.Register`** (`lib/slackex_web/live/auth_live/register.ex`): Registration LiveView with username/email/password form, real-time validation on change, redirect to login on success.
- **`SlackexWeb.AuthLive.Login`** (`lib/slackex_web/live/auth_live/login.ex`): Login LiveView with form that POSTs to session controller (action={~p"/users/log-in"}, phx-update="ignore"). Login forms must POST to a controller (not handle_event) because session cookies require a full HTTP request/response cycle.
- **`SlackexWeb.ChatLive.Index`** (`lib/slackex_web/live/chat_live/index.ex`): Main chat LiveView. Channel sidebar listing user's channels. Message stream with PubSub subscription for real-time updates. Send message form routing through `Messaging.send_message/3` (CQRS write path). Envelope-based message handling (`{:envelope, %{event: "message.new"}}`). Sender enrichment for PubSub messages (fetches user by sender_id). Auto-mark-as-read on channel selection. Welcome screen when no channel selected.
- **App layout** (`lib/slackex_web/components/layouts/app.html.heex`): daisyUI navbar with Slackex branding, current_user display, login/register/logout links, flash_group.
- **Auto-scroll JS hook** (`assets/js/hooks/message_list.js`): Scrolls to bottom on mount. On update, only auto-scrolls if user is already at bottom (100px threshold). Registered in app.js alongside colocated hooks.
- **Router updates** (`lib/slackex_web/router.ex`): `fetch_current_user` plug in browser pipeline. Two live_sessions: `:redirect_if_authenticated` (register/login) and `:require_authenticated_user` (chat). Session controller routes for POST login and DELETE logout.

### Test coverage
- 211 tests (0 failures), mix credo --strict (0 issues), mix compile --warnings-as-errors clean, mix format clean
- 17 new behavioral tests:
  - Auth LiveView tests (10): registration form render, valid registration redirect, duplicate username error, weak password validation, login form render, login-to-register link, auth redirects (unauth→login, auth→chat×2, landing page redirect)
  - Chat LiveView tests (7): channel sidebar display, channel header with description, message display, send message form, real-time PubSub message from another user (envelope broadcast), unauth redirect to login, welcome message when no channel selected

### Lessons learned

25. **Login forms must POST to a controller, not LiveView**: LiveView runs over WebSocket. Session cookies can only be set during an HTTP request/response cycle. Login forms use `action={~p"/users/log-in"}` with `phx-update="ignore"` to submit as a standard form POST to UserSessionController, which calls `Plug.Conn.put_session/3`. Registration can stay in LiveView (redirect to login page on success).

26. **Layout template embedding conflict**: Phoenix's `embed_templates` macro embeds all `.heex` files matching the pattern. If you define both an inline function component `def app(assigns)` AND a template file `layouts/app.html.heex`, the inline function shadows the template. Use `embed_templates "layouts/*"` to embed all layout templates and remove inline definitions.

27. **LiveView streams for efficient message lists**: Using `stream(:messages, messages)` in mount and `stream_insert(:messages, enriched_message)` in PubSub handlers gives Phoenix's diffing engine efficient DOM updates. Each message needs a unique DOM ID (`:dom_id` option or `id` field).

28. **PubSub message enrichment**: ChannelServer broadcasts message maps without preloaded associations. The LiveView must enrich incoming PubSub messages by fetching the sender user: `Accounts.get_user!(message.sender_id)`. This is acceptable since PubSub messages arrive one at a time (not in bulk).

29. **ConnCase test helpers for LiveView auth**: `log_in_user/2` generates a session token via `Accounts.generate_user_session_token/1` and puts it in the test session via `init_test_session/2` + `put_session(:user_token, token)`. This mirrors what UserAuth does in production without needing HTTP roundtrips.

30. **Phoenix 1.8 colocated hooks break esbuild if unused**: Phoenix 1.8 scaffolds `app.js` with `import {hooks as colocatedHooks} from "phoenix-colocated/slackex"`. If no colocated hooks exist, this module is never generated and esbuild fails silently in dev (no error in terminal). LiveSocket never connects, so LiveView forms fall back to standard HTTP POST to the current URL, producing confusing "no route found" errors. Fix: remove the import and the spread from the hooks object if not using colocated hooks. **Critical**: `mix test` does NOT catch this — LiveView tests bypass JavaScript entirely. Always run `mix assets.build` after LiveView/JS changes to verify the frontend compiles.

### What's next
- Phase 3: Scalability (time-partitioned messages, Redis caching, connection pooling)
- Phase 4: Search (full-text search, pgvector embeddings)
- Boundary compile-time checks, Tidewave setup, git hooks

---

## Phase 3 Distribution — Steps 1 + 2.1/2.3/2.4: libcluster & Horde (2026-02-22)

### What was built
- **libcluster integration** (`config/config.exs`, `config/dev.exs`, `config/test.exs`, `config/runtime.exs`): Automatic BEAM cluster formation. Gossip strategy for local multi-node dev, K8s DNS strategy for production (configurable via env vars), empty topologies for test.
- **`Slackex.NodeListener`** (`lib/slackex/node_listener.ex`): GenServer monitoring node join/leave events via `:net_kernel.monitor_nodes/2`. Logs cluster membership changes for observability.
- **`Slackex.Messaging.ChannelRegistry`** (`lib/slackex/messaging/channel_registry.ex`): Horde.Registry wrapper with `members: :auto` for CRDT-based distributed process registration. Public API: `via/1`, `via_dm/1`, `lookup/1`, `count/0`. Composite keys `{:channel, id}` / `{:dm, id}` prevent ID collisions.
- **`Slackex.Messaging.ChannelSupervisor`** (`lib/slackex/messaging/channel_supervisor.ex`): Rewritten to use Horde.DynamicSupervisor with `process_redistribution: :active` for automatic rebalancing on node changes. `ensure_started/2` uses Horde.Registry lookup + Horde.DynamicSupervisor.start_child.
- **`Slackex.Messaging.ChannelServer`** (`lib/slackex/messaging/channel_server.ex`): Updated `via_tuple/2` from stdlib Registry to Horde.Registry via tuples.
- **`Slackex.Messaging`** (`lib/slackex/messaging/messaging.ex`): Updated `get_recent_messages/2` and `channel_count/0` from stdlib Registry to Horde.Registry.
- **Application supervisor** (`lib/slackex/application.ex`): Added `{Cluster.Supervisor, ...}` and `Slackex.NodeListener` before PubSub. ChannelRegistry and ChannelSupervisor now use Horde child specs.
- **Writer epoch migration** (`priv/repo/migrations/20260222175604_add_writer_epoch.exs`): Added `writer_epoch` integer column (default 0, NOT NULL) to both `channels` and `dm_conversations` tables. Forward-looking for Step 2.2 (writer fencing).
- **Dependencies**: Added `{:horde, "~> 0.9"}` (installed 0.10.0), `{:libcluster, "~> 3.4"}` (installed 3.5.0).

### Test coverage
- 220 tests (0 failures), mix credo --strict (0 issues), mix compile --warnings-as-errors clean
- 9 new tests:
  - NodeListener (5): start_link, nodeup logging, nodedown logging, multiple events, initial state
  - ChannelRegistry (4): via/1 tuple format, via_dm/1 tuple format, lookup miss for channel, lookup miss for DM
- All 211 existing tests updated and passing after Horde migration (3 test files updated: messaging_test, channel_server_test, cache_warmer_test)

### Steps deferred
- **Step 2.2 (Writer Fencing)**: Requires epoch-aware BatchWriter + ChannelServer integration. Migration is ready (writer_epoch columns added).
- **Step 2.5 (Process Handoff/Crash Recovery)**: Requires terminate/2 flush + init/1 reconciliation. Needs LocalCluster for distributed testing.

### Lessons learned

31. **Horde is a drop-in replacement for stdlib Registry/DynamicSupervisor**: The API is nearly identical. `{:via, Registry, {Name, key}}` becomes `{:via, Horde.Registry, {Name, key}}`. `DynamicSupervisor.start_child` becomes `Horde.DynamicSupervisor.start_child`. The main difference is configuration: `members: :auto` for automatic cluster discovery and `process_redistribution: :active` for rebalancing.

32. **Test files reference supervisor/registry APIs directly**: When migrating from stdlib to Horde, test files that call `DynamicSupervisor.terminate_child`, `DynamicSupervisor.which_children`, or `Registry.lookup` must also be updated. These are not caught by `mix compile` — they only surface when running `mix test`.

33. **`replace_all` can double-prefix existing Horde references**: If a file already has some `Horde.DynamicSupervisor` references and you do `replace_all` of `DynamicSupervisor` → `Horde.DynamicSupervisor`, the existing ones become `Horde.Horde.DynamicSupervisor`. Always check for pre-existing prefixed references before bulk replacement, or do a targeted replace instead.

34. **Horde.DynamicSupervisor child_spec needs explicit members**: Unlike stdlib DynamicSupervisor which just needs `name:`, Horde requires `members: :auto` (or explicit member list) in the child spec. Without it, the supervisor starts but doesn't participate in the distributed cluster.

### What's next
- Phase 3 Step 2.5: Process handoff and crash recovery
- Phase 3 Step 3: Redis cross-node cache
- Phase 3 Steps 4-8: Push notifications, partitioning, reconnection, K8s deployment

---

## Phase 3 Distribution — Step 2.2: Writer Fencing (2026-02-22)

### What was built
- **`Slackex.Pipeline.BatchWriter`** (`lib/slackex/pipeline/batch_writer.ex`): Replaced `insert_batch/1` with epoch-fenced `insert_batch/2(messages, opts)`. Required keyword opts: `epoch:` (caller's writer epoch), `type:` (`:channel` or `:dm`), `id:` (channel/DM integer ID). Insert is wrapped in `Repo.transaction` with `SELECT writer_epoch FROM {table} WHERE id = $1 FOR UPDATE` row lock. If `db_epoch > caller_epoch`, transaction rolls back with `{:error, :epoch_stale}`. Updated `async_insert_batch` to 3-arg signature `async_insert_batch(messages, caller_ref, opts)` passing epoch opts through. Old `/1` arities removed — all callers must provide epoch.
- **`Slackex.Messaging.ChannelServer`** (`lib/slackex/messaging/channel_server.ex`): Acquires writer epoch atomically in `init/1` via `UPDATE {table} SET writer_epoch = writer_epoch + 1 WHERE id = $1 RETURNING writer_epoch`. Epoch stored in GenServer state. All three BatchWriter call sites (batch_flush, retry, idle timeout) updated to pass `epoch_opts(state)`. New `handle_info({:batch_result, _ref, {:error, :epoch_stale}}, state)` handler: emits `[:slackex, :channel_server, :epoch_stale_shutdown]` telemetry, drops all pending/in-flight writes, terminates with `{:stop, :normal, state}`. Stale guard in `send_message` returns `{:error, :not_writer}` when `state.stale == true`.

### Test coverage
- 230 tests (0 failures), mix credo --strict (0 issues), mix compile --warnings-as-errors clean
- 10 new tests:
  - BatchWriter (4 new, 9 updated): epoch-fenced insert succeeds when epoch matches, epoch stale returns error, transaction atomicity (0 messages on stale), async passes epoch through
  - ChannelServer (6 new): writer_epoch acquired as positive integer on startup, stale flag false on startup, writer_epoch incremented in DB, :not_writer returned when stale flag set, epoch_stale causes graceful shutdown, epoch_stale emits telemetry event

### Lessons learned

35. **`FOR UPDATE` row lock inside Repo.transaction eliminates TOCTOU races**: The epoch check and batch insert must happen atomically. Without `FOR UPDATE`, a concurrent writer could increment the epoch between the SELECT and INSERT, allowing a stale writer to persist messages. The row lock serializes access — only one transaction can hold the lock at a time.

36. **Epoch_stale tests must use dedicated channels**: Tests that trigger `{:stop, :normal, state}` on a ChannelServer cause Horde to attempt a restart. The restarted process tries to acquire an epoch (DB call) using the dying test's sandbox connection, which crashes. Fix: create a fresh channel per destructive test instead of using the shared setup channel. This isolates the blast radius of process termination.

37. **Credo catches `Ecto.Adapters.SQL.query!` as a nested module call**: When using raw SQL via `Ecto.Adapters.SQL.query!/3`, Credo's alias-usage check flags it. Add `alias Ecto.Adapters.SQL` at the top of the module and use `SQL.query!/3` instead.

38. **Writer epoch as a fencing token is a classic distributed systems pattern**: The epoch acts as a monotonic "fencing token" — each new writer gets a strictly higher epoch, and the database rejects writes from stale epochs. This makes the single-writer invariant verifiable rather than merely assumed. Unlike distributed locks (which can expire), the epoch persists in the database and is checked at write time.

### What's next
- Phase 3 Step 2.5: Process handoff and crash recovery (terminate/2 flush + init/1 reconciliation)
- Phase 3 Step 3: Redis cross-node cache
- Phase 3 Steps 4-8: Push notifications, partitioning, reconnection, K8s deployment

---

## Code Review Fixes — Auth Gap, Queue Ordering, N+1 Sender Enrichment (2026-02-22)

### What was fixed

Three issues identified during code review, addressed in a single commit:

1. **Auth gap in ChatLive.Index handle_params** (`lib/slackex_web/live/chat_live/index.ex`): `handle_params(%{"slug" => slug}, ...)` loaded any channel by slug without checking membership — private channel content was exposed to any logged-in user. Added `authorize_channel/2` private helper implementing layered policy: role holders use `Permissions.can?/2`, non-members can view public channels read-only (no message form), non-members are redirected from private channels with flash error. Message form gated by `:can_send` assign. `send_message` event handler also guards on `can_send`. Extracted `enter_channel/3` to keep nesting Credo-clean.

2. **Queue ordering in ChannelServer DB rehydration** (`lib/slackex/messaging/channel_server.ex`): `load_from_db/2` called `Chat.list_messages` / `Chat.list_dm_messages` which use `order_by: [desc: m.id]` (newest-first). The cache path already returned oldest-first, but the DB path fed newest-first into the queue. Added `|> Enum.reverse()` to both `load_from_db/2` clauses so both paths return oldest-first, matching the queue's chronological contract.

3. **N+1 sender queries in LiveView** (`lib/slackex/messaging/channel_server.ex`, `lib/slackex_web/live/chat_live/index.ex`): Every PubSub message triggered `Accounts.get_user!/1` in every connected LiveView — N subscribers = N identical DB queries. Moved sender lookup to ChannelServer write time (1 query per message). Added `serialize_sender/1` helper producing `%{id, username, display_name, avatar_url}` map. `message_to_map/1` preserves sender from preloaded Ecto structs on DB rehydration. LiveView `enrich_message/1` short-circuits when sender map is already present, keeping DB fallback for edge cases.

### Test coverage
- 239 tests (0 failures), mix credo --strict (0 issues), mix compile --warnings-as-errors clean
- 9 new tests:
  - Channel authorization (4): non-member redirected from private channel, non-member views public channel, non-member sees no message form on public channel, member sees message form
  - Sender enrichment (3): send_message returns serialized sender, broadcast includes sender, DB-rehydrated messages include sender
  - DB rehydration ordering (1): messages in ascending ID order after DB rehydration
  - Pre-enriched PubSub (1): pre-enriched message renders sender name in LiveView

### Lessons learned

39. **Authentication vs authorization in LiveView**: `on_mount` handles authentication (is the user logged in?), but `handle_params` must handle authorization (can this user see this channel?). `handle_params` fires on every `live_patch` navigation, so authorization must be checked each time — not just on initial mount.

40. **Write-time enrichment eliminates N+1 in PubSub fan-out**: When a single producer broadcasts to N subscribers, each subscriber performing a DB lookup creates O(N) queries for the same data. Moving the lookup to the producer (ChannelServer) reduces this to O(1). The serialized map (not the full User struct) keeps PubSub payloads lean and avoids leaking sensitive fields like `hashed_password`.

41. **DB query ordering vs queue ordering**: SQL `ORDER BY id DESC LIMIT N` is the efficient way to get the N most recent rows, but the result set is newest-first. Any consumer expecting chronological order (oldest-first) must reverse. When two code paths (cache vs DB) feed the same queue, both must produce the same ordering.

### What's next
- Phase 3 Step 3: Redis cross-node cache
- Phase 3 Steps 4-8: Push notifications, partitioning, reconnection, K8s deployment

---

## Phase 3 Distribution — Step 2.5: Crash Recovery (2026-02-22)

### What was built
- **`terminate/2` graceful flush** (`lib/slackex/messaging/channel_server.ex`): Added `Process.flag(:trap_exit, true)` in `init/1` and `terminate/2` callback that does a synchronous `BatchWriter.insert_batch` flush of `pending_writes`. Epoch check still applies — stale writers' flushes are safely rejected. The flush is wrapped in `try/rescue` to prevent crashes during termination (best-effort). Skipped when `state.stale == true` (already rejected).
- **`init/1` crash recovery reconciliation** (`lib/slackex/messaging/channel_server.ex`): After cache load and epoch acquisition, `reconcile_cache/4` compares cache message IDs against the database. Messages found in cache but missing from DB are re-persisted via synchronous `BatchWriter.insert_batch` using the freshly acquired epoch. Only runs when messages came from cache (`:cache` source tag), skipped for DB-loaded messages (`:db` source). Emits `[:slackex, :channel_server, :crash_recovery]` telemetry with `%{recovered_count, channel_id}`.
- **Source-tagged init flow**: Cache lookup now returns `{:cache, messages}` or `{:db, messages}` tagged tuples, enabling the reconciliation logic to distinguish between cached (potentially stale) and DB-loaded (already persisted) messages.

### Test coverage
- 246 tests (0 failures), mix credo --strict (0 issues), mix compile --warnings-as-errors clean
- 7 new behavioral tests:
  - terminate/2 graceful flush (3): pending_writes flushed to DB on shutdown, empty pending_writes doesn't crash, stale epoch flush safely rejected
  - crash recovery (4): un-persisted messages recovered from cache on restart, crash_recovery telemetry emitted with recovered_count, reconciliation skipped when cache empty (DB source), server survives recovery and remains functional

### Lessons learned

42. **`terminate/2` requires `Process.flag(:trap_exit, true)`**: GenServer's `terminate/2` is only called during supervisor shutdown if the process traps exit signals. Without it, the supervisor sends `Process.exit(pid, :shutdown)` and the process dies immediately — `terminate/2` never runs. This is a classic OTP gotcha: the callback exists in the module but is silently skipped. Always add `Process.flag(:trap_exit, true)` in `init/1` when you need `terminate/2` for cleanup.

43. **Defensive `terminate/2` must never crash**: Since `terminate/2` is a last-chance cleanup, any exception (e.g., DB connection gone, malformed data) would produce confusing GenServer terminating error logs. Wrap the entire flush in `try/rescue` and log the outcome. Best-effort means: try, log, never crash.

44. **Source-tagging for conditional reconciliation**: When `init/1` loads messages from two paths (cache vs DB), the reconciliation logic needs to know which path was used. Tagging the result as `{:cache, messages}` vs `{:db, messages}` at the decision point cleanly separates the concern — pattern matching on the tag in `reconcile_cache/4` eliminates runtime conditionals and makes the skip-DB-path optimization zero-cost.

45. **Cache-vs-DB reconciliation closes the durability gap**: Between batch flushes (every 2s), messages exist only in `pending_writes` and ETS cache. If the process crashes, `pending_writes` is lost but ETS survives (same node). On restart, comparing cache IDs against DB and re-persisting the difference recovers these messages. The freshly acquired epoch ensures the reconciliation insert is authorized. This pattern works for same-node crashes; cross-node crashes rely on Horde restart + the same reconciliation flow.

### What's next
- Phase 3 Step 3: Redis cross-node cache (pool, cascade, write-through)
- Phase 3 Steps 3.5-8: Read replica, push notifications, partitioning, reconnection, K8s deployment

---

## Phase 3 Distribution — Step 3: Redis Cross-Node Cache (2026-02-22)

### What was built
- **`Slackex.Cache.Redis`** (`lib/slackex/cache/redis.ex`): Supervisor managing a pool of 10 named Redix connections (`:redix_0` through `:redix_9`) for cross-node cache sharing. Random dispatch via `:rand.uniform/1`. All commands wrapped in `try/rescue` for graceful degradation — returns `{:miss, []}` or `:error` rather than raising when Redis is unavailable. Public API: `get_messages/1` (LRANGE 0 199), `push_message/2` (RPUSH + LTRIM + EXPIRE, 100ms timeout), `cache_messages/2` (pipeline DEL + RPUSH all + EXPIRE), `invalidate/1` (DEL), `set_read_cursor/3` (SET EX 86400), `get_read_cursor/2` (GET). Messages serialized as JSON strings in Redis lists (Jason.encode!/decode!). DateTime values encoded as ISO8601 strings, decoded back to DateTime via `DateTime.from_iso8601/1`. String keys atomized on decode for known fields only (`:id`, `:content`, `:sender_id`, `:inserted_at`, `:channel_id`, `:dm_conversation_id`, `:sender`). Telemetry event `[:slackex, :cache, :redis_write_timeout]` emitted on write timeout. Redis keys namespaced: `msgs:channel:{id}`, `msgs:dm:{id}`, `cursor:{uid}:{type}:{id}`.
- **`Slackex.Cache`** (`lib/slackex/cache/cache.ex`): Plain module facade coordinating 3-tier cascade. `get_messages/1`: ETS hit → return; Redis hit → backfill ETS + return; both miss → `{:miss, []}`. `put_message/2`: write-through to ETS (synchronous) then Redis (best-effort, 100ms timeout). `invalidate/1`: clears both ETS and Redis. `cache_messages/2`: bulk backfills both layers.
- **Application supervisor** (`lib/slackex/application.ex`): Added `Slackex.Cache.Redis` between `Cache.Local` and `ChannelRegistry`.
- **`Slackex.Messaging.ChannelServer`** (`lib/slackex/messaging/channel_server.ex`): Switched from `Cache.Local` alias to `Cache` facade. `init/1` cache check and `send_message` write both use `Cache` (3-tier) instead of ETS-only.
- **`Slackex.Search.HistoryLoader`** (`lib/slackex/search/history_loader.ex`): Switched from `Cache.Local` to `Cache` facade. DB miss backfill now uses `Cache.cache_messages/2` (bulk backfill both layers). Fixed `struct_to_map/1` to properly deep-convert Ecto structs: skips `__meta__`, drops `%Ecto.Association.NotLoaded{}` values, recurses into loaded associations, and short-circuits on Jason-native types (DateTime, NaiveDateTime, Date, Time) to avoid exposing internal tuple fields like `microsecond: {value, precision}`.
- **docker-compose.yml**: Redis service (redis:7-alpine, port 6379) was already defined; started alongside Postgres for test/dev environments.

### Test coverage
- 277 tests (0 failures), mix credo --strict (0 issues), mix compile --warnings-as-errors clean, mix assets.build clean
- 46 new behavioral tests:
  - `Cache.Redis` (31 tests): push/get round-trip, DateTime ISO8601 round-trip, insertion order, trim to 200, channel/DM independence, bulk backfill, DEL on re-cache, invalidate, read cursor round-trip, multi-user/multi-type cursor isolation, graceful degradation shapes
  - `Cache` facade (15 tests): ETS hit short-circuits Redis, Redis hit backfills ETS, full miss, write-through to both layers, successive writes visible in both, invalidate clears both, bulk backfill both layers, subsequent read hits ETS

### Lessons learned

46. **`Map.from_struct` exposes internal Elixir struct fields**: `DateTime`'s `microsecond` field is `{value, precision}` — a raw tuple. Recursing into a `DateTime` with `Map.from_struct` produces a map with `microsecond: {750000, 6}` that Jason cannot encode. Always guard recursive struct-to-map conversion against types that Jason already handles natively (DateTime, NaiveDateTime, Date, Time, Decimal).

47. **Redis pool with random dispatch is simpler than nimble_pool for this use case**: A Supervisor with N named Redix connections + `:rand.uniform(N)` dispatch gives connectionpool-like load distribution without the complexity of a proper pool implementation. Suitable for Redis since connections are cheap and lightweight.

48. **`try/rescue` → implicit try for Credo compliance**: Credo prefers implicit try syntax (rescue at function level) over explicit `try do...rescue...end` blocks. `defp f() do Redix.command(...) rescue e -> {:error, e} end` is idiomatic; explicit `try` is flagged by `Credo.Check.Readability.PreferImplicitTry`.

49. **Credo nesting depth**: Pattern matching via extracted helper clauses eliminates deep nesting. Instead of `fn {k, v} -> if ... case ...`, extract `defp convert_kv/1` and `defp convert_value/2` to keep each clause at depth ≤ 2.

50. **ETS remains authoritative for hot data**: Redis writes are best-effort (100ms timeout, fire-and-forget on failure). ETS is always written first and is the source of truth within a node. Redis serves as a warm cache for newly started nodes or nodes with cold ETS. This prevents Redis latency from affecting the message broadcast path.

### What's next
- Phase 3 Step 3.5: Read replica routing
- Phase 3 Steps 4-8: Push notifications, partitioning, reconnection, K8s deployment
