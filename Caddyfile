# This block is added to the host-level Caddy at /opt/caddy/Caddyfile.
# The host Caddy handles SSL via Cloudflare and proxies to the app container
# on the shared "proxy" Docker network.
#
# chat.davewil.dev {
#     reverse_proxy slackex-app-1:4000
# }
